<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/newActionBarStyle</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="title">
        <item name="android:textColor">@color/title</item>
        <item name="android:textSize">30sp</item>
    </style>

    <style name="newActionBarStyle">
        <item name="android:background">#000</item>
    </style>

    <style name="detailCreate">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#000</item>
        <item name="android:textColorHint">#000</item>
    </style>

    <style name="detailCreateTitles" parent="detailCreate">
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="generalImageButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:minHeight">@dimen/button_min_size</item>
        <item name="android:minWidth">@dimen/button_min_size</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">0dp</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <style name="deleteButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="deleteButtonRed" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">#FFF</item>
        <item name="android:background">@color/colorAccent</item>
    </style>

</resources>
